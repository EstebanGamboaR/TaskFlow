// routes/index.js — Route registry
//
// This file will import and export all route modules.
// As you build each feature, uncomment the corresponding lines.
//
// Current status:
//   [x] Health check (in app.js)
//   [ ] Auth routes     ← Phase 1: Build next
//   [ ] Project routes  ← Phase 2
//   [ ] Task routes     ← Phase 3

// const authRoutes    = require('./auth');
// const projectRoutes = require('./projects');
// const taskRoutes    = require('./tasks');

// module.exports = { authRoutes, projectRoutes, taskRoutes };

module.exports = {}; // Placeholder — will be filled in as routes are built
